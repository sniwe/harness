use `ponytail` skill for all code authoring

with each non-trivial code edit in parent dir of this file (extends to all child dirs recrusive), perform automatic git add commit push to update github repo

`::gacp` is a bootstrap command shorthand instructing "git add commit push"

any request for a plan is to be interpeted as asking for `a detailed phased vertical tracer bullet style implemention plan` to be outputted as an .md file to the project's dev subdir dated for the current date (or subdir therein if specified), e.g. C:\harness\mgmt\dev\260907