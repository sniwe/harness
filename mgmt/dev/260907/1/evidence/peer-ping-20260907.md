# Peer-ping live evidence

Date: 2026-09-07  
Local key: `machine-base-73182d23f660c3880e7e`  
Peer key: `machine-base-caf45342b1d0acc9b938`

## Focused automated proof

`npm test`: 13 passed, 1 skipped (Linux-only identity case on Windows).

Peer client tests cover exact-key lookup, URL validation, successful pong
identity validation, local-key rejection, and missing-peer handling.

## Live public proof

- Peer registry lookup returned exactly one record for each key.
- Peer route returned HTTP 200:
  `https://hollow-something-tradition-twenty.trycloudflare.com/api/machine-base/ping`
- Forward local -> peer `POST /api/machine-base/peer-ping` returned HTTP 200 in
  899 ms and validated the peer key and `serverRole=machine-base`.
- The local tunnel rotated from
  `https://seek-belkin-administrators-kansas.trycloudflare.com` to
  `https://designer-rising-pixel-luis.trycloudflare.com` under the same local
  key.
- Reverse peer -> local `POST /api/machine-base/peer-ping` returned HTTP 200 in
  1210 ms and validated the rotated local URL/key.
- After the temporary local server stopped, the peer returned HTTP 502 for the
  stale local record, proving bounded visible failure.

All temporary local worker and tunnel processes were stopped after each live
probe.
